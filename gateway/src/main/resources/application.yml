server:
  port: 8080

spring:
  graphql:
    graphiql:
      enabled: true
    cors:
      allowed-origins: "http://localhost:5173"
  threads:
    virtual:
      enabled: true

grpc:
  client:
    t-api-server:
      address: 'static://localhost:9090'
      negotiation-type: plaintext